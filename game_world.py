import sys
from PySide6.QtWidgets import QApplication, QMainWindow, QTabWidget, QWidget, QVBoxLayout, QTextEdit, QDockWidget, QComboBox
from game_world import GameWorld
from npc_manager import NPCManager, NPC



# Define the NPC class (npc_manager.py)
class NPC:
    def __init__(self, name):
        self.name = name

if __name__ == "__main__":
    main()
